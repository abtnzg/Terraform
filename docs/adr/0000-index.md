# ADR-0001: Cluster topology — hub-spoke GitOps-of-GitOps
# ADR-0002: CI/CD orchestrator — Argo Workflows + ArgoCD
# ADR-0003: SLSA target — L3 by phase 2, L4 by phase 3
# ADR-0004: Policy engine — Conftest in CI + Gatekeeper/Kyverno at admission
# ADR-0005: Secret management — Vault per region with KMS auto-unseal
# ADR-0006: Observability — Grafana stack (Mimir + Loki + Tempo) per region
# ADR-0007: Tenancy — namespace-per-team with team AppProjects
# ADR-0008: Ingress → Gateway API + Envoy Gateway (supersedes Ingress NGINX)
# ADR-0009: Karpenter + Bottlerocket + Brupop, Kyverno alongside Gatekeeper
# ADR-0010: Storage strategy — EBS, EFS, S3 Mountpoint, with Kyverno enforcement
# ADR-0011: Sizing profiles — sandbox, standard, production
